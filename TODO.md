# Project TODO

This file mirrors the assistant-tracked TODO list for the Dino TS project.

- [x] Scaffold project files (`index.html`, `styles/style.css`, `src/main.ts`, `src/assets.ts`)
- [x] Add `tsconfig.json`
- [x] Update `package.json` scripts for Vite (`dev`, `build`, `preview`)
- [x] Add `README.md` with quick start
- [ ] Run package manager install to install dev dependencies (`vite`, `typescript`)
- [ ] Decide on package manager and regenerate lockfile (commit only one lockfile)
- [ ] Implement game modules: `Player`, `Obstacle`, `Game`, `Input`
- [ ] Add asset sprites to `assets/` and an `AssetLoader` to load them
- [ ] Polish styling and responsive canvas scaling
- [ ] Add tests or simple smoke checks (optional)
