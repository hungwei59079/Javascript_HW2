# Dino TS — Chrome Dino (TypeScript)

Development scaffold for a TypeScript recreation of the Chrome Dino game.

Quick start:

1. Install dependencies:

```bash
npm install
```

2. Run dev server:

```bash
npm run dev
```

The dev server uses Vite. Open the URL shown in the terminal (usually http://localhost:5173).
